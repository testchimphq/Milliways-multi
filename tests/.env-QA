# SmartTests QA env (loaded via TESTCHIMP_ENV=QA → .env-QA)
# Reporter ingest: TESTCHIMP_API_KEY + TESTCHIMP_BACKEND_URL from .cursor/mcp.json (run-mobilewright-with-mcp-env.mjs)
MILLIWAYS_API_BASE_URL=http://localhost:3001
